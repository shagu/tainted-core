-- Add a title to indicate the update process. This is also a placeholder to
-- prevent from errors, while updating after a release when folders are still empty.
SELECT '' AS 'Installing Updates...';
